{
	"name": "mock-api",
	"version": "1.0.0",
	"scripts": {
		"start": "json-server --watch products.json --host 0.0.0.0 --port 5000 --static ./"
	},
	"dependencies": {
		"json-server": "^1.0.0-beta.3"
	}
}
